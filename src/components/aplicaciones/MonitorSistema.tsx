
import React from "react";
import MonitorSistemaComponent from "@/components/MonitorSistema";

// This is a wrapper component to render the MonitorSistema component in the apps folder
const MonitorSistema: React.FC = () => {
  return <MonitorSistemaComponent />;
};

export default MonitorSistema;
